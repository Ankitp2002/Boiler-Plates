streamlit pandas numpy scikit-learn xgboost imbalanced-learn shap plotly joblib requests

1. mkdir credit_risk_system && cd credit_risk_system
2. pip install -r requirements.txt
3. python setup.py # Download data
4. python trainer.py # Train models
5. streamlit run app.py # http://localhost:8501
