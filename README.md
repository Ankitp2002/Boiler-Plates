# Boiler-Plates

Folder Structure | Code Architecture

Must You have python3.14

# crete vertual environment with respective to OS

Window venv command > python -m venv venv .
Linux venv command > python3 -m venv .venv .
